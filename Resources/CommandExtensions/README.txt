# Placeholder so CommandExtensions is copied into the app bundle.
# Compile "Finder - Open in Terminal.applescript" to .scpt with:
#   osacompile -o "Finder - Open in Terminal.scpt" "Finder - Open in Terminal.applescript"
